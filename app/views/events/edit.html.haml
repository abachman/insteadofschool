%h1 Editing event
- form_for(@event) do |f|
  = f.error_messages
  = render :partial => 'form', :locals => {:f => f}
  %p
    = f.submit 'Update'
= link_to 'Show', @event
|
= link_to 'Back', events_path
